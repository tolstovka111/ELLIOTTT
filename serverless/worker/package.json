{
  "name": "delta-vpn-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless VLESS-over-WebSocket entry point for Dельта VPN",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
